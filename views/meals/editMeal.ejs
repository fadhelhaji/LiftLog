<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Meal</title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Chewy&family=Lobster&display=swap" rel="stylesheet">
</head>

<body>
  <div class="navbar-container">
    <div class="navbar"><%- include('../partials/_navbar.ejs') %></div>
  </div>
  <div class="edit-meal-container">
    <h1 class="edit-meal-title">Edit <%= meal.mealName %>
    </h1>

    <form class="edit-meal-form" action="/meals/<%= meal._id %>?_method=PUT" method="POST">
      <div class="form-group-edit">
        <label for="mealName">Meal Name</label>
        <input type="text" name="mealName" id="mealName" value="<%= meal.mealName %>" />
      </div>

      <div class="form-group-edit">
        <label for="protein">Protein (g)</label>
        <input type="number" name="protein" id="protein" value="<%= meal.protein %>" />
      </div>

      <div class="form-group-edit">
        <label for="carbs">Carbs (g)</label>
        <input type="number" name="carbs" id="carbs" value="<%= meal.carbs %>" />
      </div>

      <div class="form-group-edit">
        <label for="fat">Fat (g)</label>
        <input type="number" name="fat" id="fat" value="<%= meal.fat %>" />
      </div>

      <div class="form-group-edit">
        <label for="calories">Calories (Kcal)</label>
        <input type="text" name="calories" id="calories" value="<%= meal.calories %>" />
      </div>

      <button type="submit" class="form-submit-btn">Update Meal</button>
    </form>

    <a class="back-dashboard-link" href="/meals">Back to Meals Dashboard</a>
  </div>
</body>

</html>